To run the bench file do follwoing;

open a new terminal and cd to the bangdb-server directory and run the server

>./bangdb-server

Now from another terminal build the bench files

> bash build-bench.sh

finally just run the bench file

> bash runapp.sh

or to put 1M items using 100 threads (clients) use

> bash runapp.sh 100 1000000 put

etc...

Note that with 0.9 release, we don't need to use command line tool to 
create db and table as it can be done programmatically

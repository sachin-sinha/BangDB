# BangDB_CSharp

The BangDB CSharp version 1.5.2 is a C# is a wrapper around the BangDB C++ DLL. This repo contains the code for CSharp wrapper and BangDB DLL. It also contains sample test programs to see how the APIs are used and also to benchmark the db.
